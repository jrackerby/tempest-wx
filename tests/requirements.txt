# What the PURE-LAYER suite needs, and nothing else -- no homeassistant, no
# vendor SDK. Declared here rather than in validate.yml because the suite is
# what has the dependency; a list kept in the workflow is a second copy that
# goes stale the moment a test grows an import.
#
# PyYAML earned its line the hard way. test_wiring.py has imported `yaml`
# since it was written, and this went unnoticed for as long as the suite only
# ever ran inside a virtualenv that happened to carry PyYAML for other reasons.
# The first run on a clean runner failed at collection: an undeclared
# dependency is invisible exactly until the code changes machines.
pytest
PyYAML
